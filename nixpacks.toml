[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd backend && npm install", "cd ../fronted && npm install"]

[phases.build]
cmds = ["cd fronted && npm run build"]

[start]
cmd = "cd backend && node server.js"